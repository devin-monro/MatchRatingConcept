class Ratinghandler {

    public void removeRating(string uuid, float amount) {

    }

    public void addRating(string uuid, float amount) {

    }

    public void setRating(string uuid, float amount) {
        
    }

    public void adjustRating(string uuid, string type) {
        // SQL code to get player by UUID
        
    }

    public Player findPlayer(string uuid) {
        Player player = new Player();
        return player.find(uuid);
    }
}